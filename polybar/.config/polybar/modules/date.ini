[module/date]
type = internal/date
interval = 1

time= "%a, %b %d %l:%M %P"

format = %{T6}%{T-} <label>
format-background = ${colors.dark}
format-foreground = ${colors.text}
format-padding = 1
label = %{T2}%time%%{T-}
